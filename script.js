const nav=document.querySelector('nav');window.addEventListener('scroll',()=>{nav.style.boxShadow=window.scrollY>20?'0 4px 18px rgba(15,23,42,.06)':'none'});
document.querySelectorAll('.wf-slider').forEach(slider=>{const track=slider.querySelector('.wf-track');const slides=[...track.children];const dots=[...slider.querySelectorAll('.wf-dots button')];let idx=0;function go(i){idx=(i+slides.length)%slides.length;track.style.transform=`translateX(-${idx*100}%)`;dots.forEach((d,di)=>d.classList.toggle('active',di===idx));}slider.querySelector('.wf-nav.prev').addEventListener('click',()=>go(idx-1));slider.querySelector('.wf-nav.next').addEventListener('click',()=>go(idx+1));dots.forEach((d,di)=>d.addEventListener('click',()=>go(di)));});